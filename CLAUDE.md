# CLAUDE.md — cc-notify plugin

State snapshot for picking up later. See @README.md for user-facing docs, @LESSONS.md for gotchas, @TODO.md for known issues.

## Status

- **Deploy after pushing to `main`** (the two commands the user runs, in order):
  1. `/plugin marketplace update farishijazi-plugins`  ← re-pulls the bumped plugin from `main` (needs the `plugin.json` version bumped, else no-op)
  2. `/reload-plugins`  ← reloads hooks into the session
  Then **new** Claude Code sessions run the new hook scripts. Editor-extension changes additionally need an editor window reload (Cmd+Shift+P → Developer: Reload Window).
- **Version**: 1.8.2 (extension 1.0.4). 1.8.1 = 1.8.0 (remote sessions, color sync) + Ghostty remote paths (@docs/devlog/claude_20260905-1300-ghostty.md). **Single source of truth = the `cc-notify` repo** (develop here, push to `main`). The marketplace `github.com/FarisHijazi/claude-plugins` references it externally (`source: {github, repo: FarisHijazi/cc-notify}`; bundled copy deleted) so there's no second copy to drift. **MUST bump `.claude-plugin/plugin.json` version on every change** or `/plugin update` no-ops and installed sessions run a frozen stale cache (this bit us — frozen 1.7.0). Latest session handoff: @docs/devlog/claude_2026-08-24-remote-sessions.md.
- **v1.8.2 fixes**: **typing into the input box never pressed Enter, on any machine** (@docs/devlog/claude_20260908-2000-nbsp-raw-and-existing-window.md, @LESSONS.md #20 + #25). Three separate bugs in one row of text: (a) Claude Code draws a **recognised slash command coloured**, and `cc-prompt-state`'s default reading drops coloured runs as decoration — so `/color orange` read back as `orange` and `/compact` as `""`, and every caller aborted one keystroke before Enter (`colorsync.log` was 100% `ABORT before Enter`); new **`--raw`** mode (escapes stripped, colours kept) is the read-back reading, the default stays the "is the user typing?" reading. (b) The ❯ and the text are separated by **U+00A0**, and an EMPTY row is exactly `❯`+U+00A0 — `[[:space:]]` matches it on macOS but NOT under glibc, so every Debian box read an empty box as "user is typing" and refused to type at all; `normalize()` folds it. (d) **Two hooks type into the same box**: SessionStart's `/color` and the auto-compact hook's `/compact` both fire on PostCompact, interleave inside each other's check-type-verify dance, and BOTH abort — `bin/cc-type-lock.sh` (new, keyed on `#{pane_id}`, stale-holder aware) serialises them; see @LESSONS.md #26. (c) A **swallowed Enter looks like success** — `cc-color-apply.sh` now waits `CC_COLOR_ENTER_DELAY` (1s) before Enter and re-presses for `CC_COLOR_CONFIRM_SECS` (10s) while the text is still in the box. Also: `cc_hub_pane` matches the host in **three tiers** (exact → alias-normalised (`user@`, `:port`, case, `.local`) → the session name alone when exactly one REMOTE watch pane carries it), because `tw-remote` builds hubs from `faris@dema-dev:~` while the bridge stamps `dema` — the exact match failed and every click materialized a SECOND window onto a session already on screen; and `cc_focus_named_terminal` (cc-lib.sh, called by cc-focus.sh before materializing) focuses a Ghostty window already attached to the session, matched on the tmux title `"<session> · <host>"`. **v1.8.2 also handles the OTHER watch topology** (@LESSONS.md #27): when you `ssh <host>` and run `tw` there, the hub is a session on that box and nothing local carries `@tw-src`, so every click materialized a second window onto an already-visible session — `cc_remote_hub_pane` finds the (attached, preferred) remote hub in one ssh, the click focuses the window titled after the HUB, and `select-pane` moves the remote grid onto the session. **`set-titles` is a per-SERVER option**: both Debian boxes had it `off` because their tmux servers predate the dotfiles line, which defeats every title-based remote focus — check `tmux show -g set-titles` on the box.
- **v1.7.0 adds**: (a) Claude orange logo on the banner via `alerter --sender com.anthropic.claudefordesktop`; (b) exact integrated-terminal-pane focus for VS Code/Cursor via the new `editor-extension/` + `bin/cc-install-editor-extension`.
- **v1.8.0 adds**: **remote sessions over SSH** (banner + click-to-focus for Claude Code on other machines). See @docs/devlog/claude_2026-08-24-remote-sessions.md + @LESSONS.md #22. Split cleanly in two: **(a) signal out** — `hooks/cc-remote-emit.sh` (installed on the remote by `bin/cc-install-remote`) appends ONE JSON line per event to `~/.claude/cc-events.jsonl`; it decides nothing, reporting only facts the remote alone knows (transcript colour/title/outcome token, tmux coords, cwd, branch). Zero deps there (sed for the hook payload, grep for the transcript — `cc_last_status_token` gained a node-less grep fallback); all JSON work happens on the Mac. **(b) routing back** — tmux-watch ALREADY solves it: a hub pane carries `@tw-src = "<host>\t<session>"`, a stable address for "the local pane showing that remote session", so `cc_hub_pane` + `cc_pane_route` (cc-lib.sh) rebuild exactly the routing a local session has, and `cc-focus.sh`'s existing machinery runs unchanged. `bin/cc-remote-bridge` holds `ssh <host> tail -F` per host (supervisor + per-host streamer, auto-reconnect, launchd via `bin/cc-install-remote-agent`) and **stamps the ssh ALIAS** on every event — the one place remote identity becomes local identity (the remote knows only its hostname; the pane is keyed by the alias). Presentation is shared, not duplicated: cc-notify.sh's facts→banner block was extracted to **`cc_present`** in cc-lib.sh and is called by both, so local and remote can't drift. Hosts are DISCOVERED from hub panes (+ `~/.claude/notify.remote-hosts`), never configured. Click re-resolves the pane at CLICK time (panes come and go); if nothing shows the session it materializes a Terminal attached to it. Bonus: **hub pane borders are now a status board** (`@cc-status` user-option + `pane-border-format`, immune to inner programs rewriting the title) — for LOCAL sessions too, written by cc-notify.sh. Off: `~/.claude/notify.disable_remote` (local) / `notify.disable_remote_emit` (remote). Log `/tmp/cc-notify/remote-bridge.log`; `cc-notify-doctor` section 7.
  - Two bugs found by testing, both fixed and both worth remembering: **(i)** `set -u` + **`$TMUX` unset under launchd** killed the bridge mid-route-write — a LaunchAgent has none of your shell's env, and the failure was silent + partial (a truncated route file, no banner). **(ii)** `aerospace focus --pid` picks the FIRST window of the pid, and **Terminal.app runs many windows under one pid** — locally that's covered by the window id captured at SessionStart, which a remote session has no equivalent of. New `cc_wid_for_tty` (cc-lib.sh) bridges AppleScript (only thing that knows which window holds a tty) and Aerospace (only thing that can focus across workspaces) **on the window TITLE**, which both report identically.
- **v1.7.17 adds**: **per-project color sync via `<cwd>/.cc/settings.json`** (key `"color"`). Research verdict (see @docs/devlog/claude_2026-08-05-color-sync.md + @LESSONS.md #21): a running session's color has NO programmatic API — the only lever is that `/color` accepts an inline argument. So: on **SessionStart**, if the settings color differs from the session's, `cc-capture-window.sh` spawns `hooks/cc-color-apply.sh <tmux-target> <color>`, which types `/color <name>` into the session's OWN pane via the `cc-prompt-state` dance (LESSONS #20: only into an empty box, verify before Enter, fail closed; ≤25s wait for the box covers startup/trust-dialog). On **UserPromptSubmit/Stop/Notification**, `cc_color_persist` (cc-lib.sh) merge-writes the transcript's color back into `.cc/settings.json` (creates the folder, preserves sibling keys, skips unchanged). Direction is one-way per event → the two writers can't race; last active session in a cwd wins. Color values are whitelist-validated at persist, read, AND actuation. Same commit: `cc_session_meta`'s color grep is now ANCHORED to `^{"type":"agent-color"` lines — the loose inline match would let a transcript that merely quotes `"agentColor":"…"` (a cc-notify dev session!) poison the settings file. Off: `CC_NO_COLOR_SYNC=1` / `~/.claude/notify.disable_color_sync`; knob `CC_COLOR_APPLY_TIMEOUT` (25); log `/tmp/cc-notify/colorsync.log`. tmux-hosted sessions only. `.cc/` is gitignored here; users should ignore it too. Bonus finding: hidden `--agent-color <color>` launch flag exists (teammate feature) but writes no transcript line → useless to us.
- **v1.7.16 adds**: **dismiss-on-typing + `bin/cc-prompt-state`**. A banner now clears the moment you start typing in the session it came from — previously you had to submit (`UserPromptSubmit`), click, or wait out the timeout. Nothing in Claude Code exposes *unsubmitted* input (no hook, env var, or API — checked), so the only observable is what the TUI drew: `bin/cc-prompt-state <tmux-target>` reads the input box off the pane via `tmux capture-pane`. It locates the box as the region between the **last two `─` rule lines** at the bottom of the screen, requiring the first row to start with `❯` — which is what makes an **AskUserQuestion menu / permission dialog report "no box" (exit 2) instead of being mistaken for an empty prompt** (a menu's `❯` selection cursor is not bracketed by the rules). Exit codes: `0` empty / `1` has text / `2` no box. `--watch <target> [timeout] [poll]` blocks until the box content CHANGES from the moment it started (so text you'd *already* typed doesn't instantly dismiss) and exits 0. `cc-notify-bg.sh` spawns one watcher per banner (target from the route file's `tmux_target`), and on a change runs the same synchronous `--remove` → `sleep 0.3` → `pkill` ordering as the click/reply paths (LESSONS #11); the watcher is killed as soon as `alerter` returns, so nothing outlives the banner (verified: no strays after both the typing path and the timeout path). tmux-hosted sessions only; others keep the reply/click paths. Off switch `CC_NO_TYPE_DISMISS=1`, poll interval `CC_TYPE_POLL` (1s). Validated against all 21 live panes at the time (empty / typing / Arabic text / menu-open) with zero misclassifications. **Also consumed by the out-of-repo `~/.claude/hooks/auto-compact-continue.sh`** — see @docs/devlog/claude_2026-08-03-prompt-state-and-autocompact.md.
- **v1.7.15 adds**: **🚨 emergency auto-focus**. On `Stop` with the 🚨 token, `cc-notify.sh` spawns `cc-focus.sh` directly (detached) — jumps straight to the session (window + Aerospace workspace + tmux pane, same path as a banner click) WITHOUT waiting for a click. The banner still fires as the visible record (focus is in ADDITION to, not instead of). Placed after the route-file write but BEFORE the Stop gating, so it fires even under `notify.disable_stop` / `notify.suppress_when_focused` (emergency overrides everything, matching the token's priority-1 semantics). Non-🚨 tokens and Notification events are unaffected. Tested via sandboxed hook copies with stubbed `cc-focus.sh`/`cc-notify-bg.sh`: 🚨-stop → focus+banner; ✅-stop → banner only; 🚨-stop with kill-switch → focus only.
- **v1.7.13/14 fixes**: **multi-GB alerter RAM leak (orphaned banner workers)**. v1.7.11's `--timeout 86400` (24h) turned every unclicked banner into a 24h-lived ~30MB `alerter` process. `kill-stale` (`cc-notify.sh`) only reaps workers for the SAME session_id, so a session that ENDS (or you walk away) with a live banner ORPHANS its worker — nothing kills it for 24h. Across hundreds of sessions/day the orphans piled to ~20GB RSS (~680 procs; each alerter is ~30MB and stable — NOT one leaking process, hundreds accumulating). The old 120s timeout self-limited to 2min so orphans never accumulated — the leak was *created* by the 24h bump. Fix: **(a)** `cc-capture-window.sh` now reaps the session's alerter on **SessionEnd** too (not just UserPromptSubmit — the precise cleanup, and the REAL orphan fix); **(b)** default timeout restored **86400 → 120** (env `CC_BANNER_TIMEOUT`) so any un-reaped walk-away orphan self-dies in 2min. Peak alerter RAM now ≈ (sessions with a banner in last 2min) × 30MB, self-limiting. Trade-off: a reply >120s after a banner appeared can't `--remove` the already-exited worker → stale banner lingers (raise `CC_BANNER_TIMEOUT` for a longer removal window at a RAM cost). See @LESSONS.md #19. NOTE: running sessions re-read hooks from disk each fire, so they pick up the 120s timeout + SessionEnd-reap immediately after the cache patch.
- **v1.7.12 adds**: **🥱 "still waiting — nothing new" token (fires NO banner)**. New outcome token for a loop/poll/schedule/monitor tick that found nothing to report — so a per-minute poll updates the tab to 🥱 but never pings. Wired the same way as the idle notification's tab-only path: `cc_last_status_token` recognizes 🥱, the Stop case sets `notif_tab_only=1` (tab updates, `exit 0` before the banner), `cc_status_emoji polling` → 🥱, and 🥱 added to `cc_set_status`'s leading-status strip run. Docs: added as priority-11 token in BOTH `docs/cc-notify-tokens.md` and the global `~/.claude/cc-notify-tokens.md` (+ README outcome table). Claude emits it only inside a running loop/poll where there's genuinely nothing new.
- **v1.7.11 fixes**: **(a) reply doesn't clear the banner; (b) Option+Shift+A hotkey dead.**
  - **(a) Clearing** — the Stop/Notification banner worker (`cc-notify-bg.sh`) blocked with `--timeout 120`. On macOS Tahoe, `alerter` can't purge an *already-delivered* notification once its poster process has exited (`alerter --list ALL` returns EMPTY even while a banner is live → the delivered-notification subsystem is non-functional; `--remove` works ONLY by closing a still-LIVE worker — confirmed via E2E). So replying **>2 min** after a banner appeared hit a dead worker → the reply's `alerter --remove`/`pkill` (in `cc-capture-window.sh` on `UserPromptSubmit`) was a no-op → banner lingered. Fix: worker timeout `120` → **`86400`** (24h, env `CC_BANNER_TIMEOUT`) so a reply always finds a live worker to close; plus `cc-notify.sh` now **kills any stale worker for the session before posting** (`pkill -f "alerter.*cc-<sid> "`) so exactly one removable worker exists per session (banner still single via `--group`). Bounded 24h so an abandoned session's worker self-cleans. Bonus: the hotkey (which only acts on sessions whose alerter is still running) now reaches banners far longer too. **NOTE: only NEW post-deploy sessions get the fixed worker; the ~57 running sessions keep their pinned 120s cache until restarted.**
  - **(a2) Dismiss race (both paths)** — dismissal ran `alerter --remove` then an IMMEDIATE `pkill` of the worker. But on Tahoe `--remove` removes the banner by telling the LIVE worker to cleanly self-close its own delivered notification (`@CLOSED`, ~50ms) — an immediate `pkill` SIGTERMs the worker mid-removal, aborting it → banner stays on screen. In `cc-banner-click` the `--remove` was even backgrounded (`&`) so `pkill` reliably won → the "Option+Shift+A navigates but the banner doesn't disappear" flake. In `cc-capture-window.sh` (reply) the immediate pkill compounded (a). Fix (both): run `--remove` synchronously, then `sleep 0.3` before the `pkill` straggler-reap (backgrounded in the hotkey so it stays snappy). Verified: worker now prints `@CLOSED` (clean removal) instead of dying to SIGTERM.
  - **(b) Hotkey** — the Karabiner rule pointed at `~/.claude/plugins/marketplaces/farishijazi-plugins/plugins/cc-notify/bin/cc-banner-click`, which **no longer exists**: the marketplace was refactored to reference cc-notify externally (`source:{github,repo:FarisHijazi/cc-notify}`, bundled copy deleted), so the script now lives ONLY in the versioned `cache/…/<version>/bin/` and the dev repo. Pressing the hotkey ran a dead path (empty `/tmp/cc-banner-click.log` = never fired). Fix (LIVE, applied to `~/.config/karabiner/karabiner.json`): `shell_command` now `bash -lc 'for p in "$HOME"/Projects/cc-notify/bin/cc-banner-click $(ls -dt "$HOME"/.claude/plugins/cache/farishijazi-plugins/cc-notify/*/bin/cc-banner-click 2>/dev/null); do [ -x "$p" ] && exec "$p"; done'` — dev repo first, else newest cache, so it survives version bumps (the exact thing that broke it). Backup at `karabiner.json.bak-<ts>`. Karabiner auto-reloaded. README's Karabiner example still shows the old dead path → update for other users.
- **v1.7.10 fixes**: **editor-extension install "cannot be installed because it was not found"**. `bin/cc-install-editor-extension` hardcoded the symlink folder name as `farishijazi.cc-notify-focus-1.0.0` while `editor-extension/package.json` was bumped to 1.0.4. VS Code/Cursor require the folder name `<publisher>.<name>-<version>` to MATCH the manifest — on mismatch the editor deems the unpacked ext invalid and tries to reinstall by id from the marketplace (never published) → the "not found" toast. Fix: installer now derives the version from `package.json` (never drifts again), removes ANY prior `farishijazi.cc-notify-focus-*` symlink before linking, and scrubs the stale `extensions.json` cache entry so the editor re-discovers cleanly. Re-run `bin/cc-install-editor-extension` after a plugin update (as always) to pick up the fixed installer. Extension unchanged (still 1.0.4).
- **v1.7.9 adds**: **sweep kill-switch** `~/.claude/notify.disable_sweep`. Gated at all 3 layers: the extension's `sweepThisWindow()` (the actuator — the definitive stop; needs a window reload to load the gated `extension.js`, but it's symlinked so no reinstall), `bin/cc-sweep` (exits before touching `.sweep`, even `--force`), and `cc_trigger_sweep` (returns early so hooks don't spawn it). `touch ~/.claude/notify.disable_sweep` to disable; `rm` to re-enable. The heartbeat LaunchAgent is a separate off-switch (`cc-install-sweep-agent --uninstall`). NOTE: deployed cache versions predating the flag don't honor it — hand-patch or redeploy (the 1.7.8 cache was hand-patched this session so running sessions stopped touching `.sweep` immediately). Extension → 1.0.4.
- **v1.7.8**: heartbeat default interval 30s → **60s** (`CC_SWEEP_AGENT_INTERVAL` still overrides).
- **v1.7.7 adds**: **60s heartbeat sweep (hook-independent)**. Hook triggers only fire in NEW post-deploy sessions and only on settled events, so tabs still drifted between turns. `bin/cc-install-sweep-agent` installs a launchd LaunchAgent (`com.farishijazi.cc-notify-sweep`, `StartInterval=60`, `RunAtLoad`) that runs `cc-sweep` every 60s regardless of any session. Each tick is just `cc-sweep`, so it obeys the SAME guards — throttled ≤1/10s, skipped while typing, **queued** if blocked. The plist points at the cc-sweep next to the installer (`$here/cc-sweep`), so **re-run after a plugin update** to re-point (like `cc-install-editor-extension`). On the dev machine it points at `~/Projects/cc-notify/bin/cc-sweep` (stable, always-latest). `--uninstall` removes it. Error log: `/tmp/cc-notify/sweep-agent.log`. Interval override: `CC_SWEEP_AGENT_INTERVAL`.
- **v1.7.6 adds**: **auto-sweep on status change + the sweep itself**. Background windows' tabs only re-render when their terminal becomes active, so a backgrounded session's tab (e.g. ⏳→✅ on `Stop`) stayed stale until you focused it. Fix = `bin/cc-sweep`: touches `/tmp/cc-notify/.sweep`, which EVERY editor window's extension watches → each runs `sweepThisWindow()` (cycles its terminals via `workbench.action.terminal.focusNext` N times, lands back on the start tab) so all tabs repaint. **Two guards** (both off with `--force`): (1) **throttle** ≤1 sweep/10s, state = `.sweep`'s own mtime (no extra file); (2) **keyboard guard** — skip if a key was pressed <3s ago (read live from the OS HID state via the compiled Swift helper `bin/cc-input-idle` → `CGEventSourceSecondsSinceLastEventType`, **no daemon, no perms, no state file**; mouse movement does NOT block). A blocked sweep is **queued, not cancelled**: a single background drainer (atomic `mkdir` lock = single-flight) polls until both guards clear, then fires once; many blocked triggers coalesce into one pending sweep. **Auto-triggered** via `cc_trigger_sweep` (cc-lib.sh, resolves `../bin/cc-sweep`) on **settled** events only — `Stop`+`Notification` (cc-notify.sh, after the tab write) and `SessionStart`+`SessionEnd` (cc-capture-window.sh); NOT mid-turn tool churn (would flicker the active window every ~10s). Env knobs: `CC_SWEEP_KBD_GUARD` (3), `CC_SWEEP_THROTTLE` (10), `CC_SWEEP_POLL` (1), `CC_SWEEP_QUEUE_MAX` (600). Extension `focusNext` sweep + `reapplyActiveTab` self-heal-on-focus were already in 1.0.3.
- **v1.7.5 fixes**: **stuck-⏳ tabs**. The `Stop`/`Notification` tab write in `cc-notify.sh` was gated `if term=vscode && shell_pids` (cc_write_tab) with NO else — but `cc_detect_terminal` is FLAKY under tmux, while the mid-turn ⏳ comes from the UNGATED cheap path (`cc_set_status` in cc-capture-window.sh). So a flaked detection on Stop left ⏳ frozen forever (deployed 1.7.1 had `cc_set_status` count=0 in cc-notify.sh — confirmed). Fix: added `else ( cc_set_status "$sid" "$status_emoji" & )` so the outcome emoji is ALWAYS swapped onto the existing `.tab`, term-detection or not. Only ever bit tmux-in-Cursor sessions. One-off resync of already-stuck tabs: loop `.tab`s starting with ⏳ → `cc_set_status` to (transcript token || ℹ️), skipping active sessions (transcript mtime <45s). See @LESSONS.md #18.
- **v1.7.4 adds**: **idle Notification is tab-only (no banner)**. CC's ~60s `idle_prompt` ("waiting for your input") now updates the tab to ❓ but spawns NO banner — `cc-notify.sh` sets `notif_tab_only=1` in the idle case and `exit 0`s before the bg worker. Permission prompts + the generic fallback still banner; instant `Stop` already covers "done". Also synced the repo `docs/cc-notify-tokens.md` (was missing 💯) + the global `~/.claude/cc-notify-tokens.md` with the rule: token emojis appear ONLY as the single trailing token, never inline/as heading marks.
- **v1.7.3 adds**: (a) **🔀 AskUserQuestion** — its multiple-choice menu fires NO Notification (only `idle_prompt`/`permission_prompt` exist), but it's a tool, so `cc-capture-window.sh` keys off `PreToolUse` with `tool_name=AskUserQuestion` → `options` status (🔀); PostToolUse falls back to ⏳. Needed adding `tool_name` to that hook's JSON parse. (b) **💯 token now recognized** — it was in `cc-notify-tokens.md` (priority 2) but missing from `cc_last_status_token`'s array, so it silently fell through to ℹ️; now detected and **displayed as 💯✅** (two-glyph, via `cc_status_emoji complete`). `cc_set_status`'s strip regex now strips a *run* of leading status emojis so the two-glyph 💯✅ is replaced cleanly by later cheap swaps. Reminder: ⏳ = working/in-progress, NOT done (done = outcome token or ℹ️/👀).
- **v1.7.2 adds**: tab title **self-heals on focus** — the extension's `onDidChangeActiveTerminal` (fires when a terminal becomes active = "you opened the session") now re-asserts that terminal's name straight from its `.tab` file (`reapplyActiveTab()`), replacing the old `pending` map. Single source of truth = the `.tab` file on disk; a background terminal whose `.tab` changed while inactive gets the right title the instant you focus it. **Decision: focus does NOT clear notifications** — clearing stays on `UserPromptSubmit` (respond), because flicking through tabs fast would silently dismiss unread notifications. Needs an editor window reload to pick up the new `extension.js`.
- **Installed**: via marketplace, autoUpdate on. Live caches at `~/.claude/plugins/cache/farishijazi-plugins/cc-notify/{1.0.0,1.3.2,1.3.3,1.4.0}/` — all have been hand-patched to v1.6.0 scripts.
- **Hotkey**: Karabiner-Elements rule binds `Option+Shift+A` → `bin/cc-banner-click`. Fires on key release (not key-down) with 150 ms settle delay + 800 ms debounce. Banner dismisses via `alerter --remove` only on successful focus.

## Architecture (3-script split + 1 capture helper)

```
hooks/cc-notify.sh         Hook entry — parses JSON, gates Stop, writes route file, spawns bg worker, exits <100ms.
hooks/cc-notify-bg.sh      Detached worker — blocks on alerter (120s timeout), on click invokes cc-focus.sh. Adds --sender for Claude icon.
hooks/cc-focus.sh          Click handler — focuses target window via Aerospace, switches tmux, focuses VS Code/Cursor terminal pane.
hooks/cc-lib.sh            Sourced library: cc_hub_pane (local watch pane by @tw-src), cc_remote_hub_pane (the hub ON the box, one ssh — LESSONS #27), cc_focus_named_terminal (reuse a window by tmux title), cc_detect_terminal (shared GUI/term + shell-pid walk), color/status emoji, session meta (color+title), tab name, cc_write_tab, cc_set_status (cheap status swap), cc_last_status_token, cc_trigger_sweep (fires ../bin/cc-sweep).
hooks/cc-capture-window.sh Status updater for 7 hooks (SessionStart/UserPromptSubmit/PreToolUse/PostToolUse/SubagentStop/PreCompact/SessionEnd). FULL update (window capture + transcript grep) only on SessionStart/UserPromptSubmit; cheap status-emoji swap (no grep) on the rest. Auto-sweeps on SessionStart/SessionEnd. Color sync: applies .cc/settings.json color on SessionStart (spawns cc-color-apply.sh), persists the session's color on UserPromptSubmit.
bin/cc-type-lock.sh        Sourceable mkdir-lock keyed on `#{pane_id}`; serialises every hook that TYPES into a pane (see LESSONS #26). Sourced by cc-color-apply.sh and by ~/.claude/hooks/auto-compact-continue.sh.
hooks/cc-color-apply.sh    Types `/color <name>` into the session's own tmux pane (the ONLY way to recolor a running session) via the cc-prompt-state safety dance. Spawned detached on SessionStart when .cc/settings.json disagrees.
bin/cc-banner-click        Hotkey handler — finds latest live banner, runs cc-focus.sh, dismisses alerter on success.
bin/cc-install-editor-extension  Symlinks editor-extension/ into ~/.vscode/extensions + ~/.cursor/extensions.
bin/cc-sweep               Tab-repaint trigger — throttled (≤1/10s) + keyboard-guarded (skip if typing <3s) + queued (single drainer, coalesces). Touches /tmp/cc-notify/.sweep.
bin/cc-input-idle(.swift)  Tiny Swift helper — prints "<kbd_idle_sec> <mouse_idle_sec>" from the OS HID state (no daemon/perms). Compiled on first use to ~/.cache/cc-notify/. Used by cc-sweep's keyboard guard.
bin/cc-install-sweep-agent Installs a launchd LaunchAgent that fires cc-sweep every 60s (hook-independent heartbeat). Re-run after a plugin update; --uninstall to remove.
hooks/cc-remote-emit.sh    THE ENTIRE REMOTE HALF. Appends one JSON facts-line per event to ~/.claude/cc-events.jsonl. No node/jq/python.
bin/cc-remote-bridge       Mac side. Supervisor (discover hosts, one streamer each) + per-host `ssh tail -F` streamer → banner + route + hub-pane status. --hosts / --test / --stop.
bin/cc-install-remote      Installs the remote half on ssh hosts (scp 2 files + merge hooks into the remote settings.json, all JSON done locally). --uninstall.
bin/cc-install-remote-agent LaunchAgent that keeps cc-remote-bridge running. Re-run after a plugin update; --uninstall.
bin/cc-prompt-state        Reads a session's Claude Code input box from its tmux pane (the only way to see UNSUBMITTED input). exit 0 empty / 1 typing / 2 no box (menu/dialog). --watch blocks until it changes. Used for dismiss-on-typing + by the autocompact hook to never type over you.
editor-extension/          VS Code/Cursor extension: focus?pids=… URI → terminal.show(); .tab watcher → rename; .sweep watcher → sweepThisWindow() (focusNext cycle, repaint all tabs).
```

State files in `/tmp/cc-notify/`:
- `<session_id>.route` — captured context (term, tmux_target, client_tty, cwd, tmux_socket, gui_pid, target_wid, editor_app, shell_pids)
- `<session_id>.window` — Aerospace window-id captured by cc-capture-window.sh (terminal/editor windows only)
- `<session_id>.tab` — `{pids,name}` the editor extension watches to set the tab name (status+color+title)
- `focus.log` — extension breadcrumb: last focus/rename/sweep action (pid/name or "no match")
- `.sweep` — touch-marker every editor window watches → triggers a tab-repaint sweep; its mtime doubles as cc-sweep's throttle clock. `.sweep.lock` — atomic mkdir lock for the single queued-sweep drainer.
- `remote-bridge.log` — bridge audit trail (streams, reconnects, banners fired). `bridge-<host>.pid` / `bridge-supervisor.pid` — streamer pids (`--stop` kills BY PID, never by pattern). `bridge-<host>.ts` — last handled event timestamp per host = the replay duplicate-guard.
- `colorsync.log` — cc-color-apply.sh audit trail (applied/skipped/aborted). The color config itself is per-project: `<cwd>/.cc/settings.json`, NOT in /tmp.

## Decisions / non-obvious mechanics

- **Window detection precedence**: captured `target_wid` (from cc-capture-window.sh) > `gui_pid` walk > AppleScript-by-tty (Terminal.app) / AppleScript-by-tmux-title (Ghostty, `cc_wid_for_tty`) > **VS Code/Cursor workspace-folder match by walking up cwd** (v1.6.0) > `open -a` last resort. Never `--reuse-window` (it opens cwd as a sub-view).
- **VS Code/Cursor terminal-pane focus** (v1.7.0): after window focus, `cc-focus.sh:focus_vscode_terminal()` fires `open "<vscode|cursor>://farishijazi.cc-notify-focus/focus?pids=$shell_pids"` — but only if the extension dir exists (else the editor pops a "not installed" toast). Scheme picked from `editor_app` (captured) or the focused window's Aerospace app-name. `shell_pids` = hook's ancestor PPID chain **plus** every pid on `client_tty` (the latter covers tmux-inside-editor, where the editor's shell == tmux client shell is a *sibling*, not an ancestor). Extension matches by `Terminal.processId ∈ shell_pids`, then `.show()`. See @LESSONS.md gotcha #13.
- **Per-session color emoji** (v1.7.0): reads `agentColor` (set by `/color`, stored as `"agentColor":"…"` in the transcript JSONL — same source `gsd-statusline.js` uses) from `transcript_path`, maps red/orange/yellow/green/blue/purple/pink/cyan → 🔴🟠🟡🟢🔵🟣🩷🩵, prefixes the banner subtitle.
- **Session name** (v1.7.0): `/rename` writes a `{"type":"custom-title","customTitle":"…"}` line to the transcript; Claude also auto-writes `{"type":"ai-title","aiTitle":"…"}`. cc-notify reads the cascade customTitle → aiTitle → cwd basename in one `grep` pass over the transcript (alongside agentColor).
- **Status emojis** (v1.7.0): tab + banner title share one vocab. **hooks.json registers 9 events** (added PreToolUse/PostToolUse/SubagentStop/PreCompact/SessionEnd) so the status self-heals/stays current — more triggers = fresher. Mapping: SessionStart→⏸️ startup, UserPromptSubmit/PreToolUse/PostToolUse/SubagentStop→⏳ running (EXCEPT `PreToolUse` with `tool_name=AskUserQuestion`→🔀 options, since its menu fires no Notification — v1.7.3), PreCompact→🗜️ compacting, SessionEnd→cleared (status removed), Notification→🔐 permission / ❓ question-input / 🔔 generic (split by `notification_type`+message; raw types logged to `notiftypes.log` — only `idle_prompt`/`permission_prompt` seen in practice). v1.7.4: the ❓ idle one (`idle_prompt`) is TAB-ONLY (sets `notif_tab_only=1`, no banner); 🔐 permission + 🔔 generic still banner, Stop→ outcome (💯✅/✅/❌/… from the message token, else 👀). v1.7.12: the 🥱 outcome token ("still waiting — nothing new", for loop/poll/schedule ticks) is ALSO tab-only (`notif_tab_only=1`, no banner). High-frequency hooks use `cc_set_status` (swap the leading status emoji on the existing `.tab`, NO transcript grep / aerospace call, detached) so they don't slow tool calls; only SessionStart/UserPromptSubmit do the full grep. Colored circles stay reserved for `/color` identity. Format: `<status> <color> <name>`. NOTE: hooks.json changes need a new Claude Code session to take effect.
- **Resume names the tab too** (v1.7.0): detection lives in `cc_detect_terminal` (cc-lib.sh), used by BOTH cc-notify.sh and cc-capture-window.sh. cc-capture-window self-detects the terminal + shell pids (no longer reads them from the route file), so `claude --resume` — a new terminal with no prior route — still names its tab at SessionStart. PreToolUse/PostToolUse stay cheap (cc_set_status, no detection). Was: capture-window depended on cc-notify.sh's route, so resumed sessions had stale/no pids until the first turn-end.
- **Tab status decoupled from banner** (v1.7.0): the `.tab` write happens in `cc-notify.sh` BEFORE the Stop banner gating (and `cc_write_tab` is no longer in the bg worker), so the tab updates even when the banner is suppressed/kill-switched. This was the "stuck on ⏳" bug — the tab write used to live in the gated bg path. Status flows from all 4 registered hooks (SessionStart/UserPromptSubmit via cc-capture-window; Notification/Stop via cc-notify). NOTE: stuck tabs from before need a one-time editor reload (the file-watcher's activation pass re-applies current `.tab` files).
- **Turn outcome on Stop** (v1.7.0): the global `~/.claude/CLAUDE.md` instructs Claude to end every message with a trailing token: `✅` task done / `❌` task failed / `👍` good news / `👎` bad news / `💬` neutral (priority: task outcome → good/bad sentiment → neutral). `cc_last_status_token` reads the last text-bearing assistant message and returns that emoji → Stop status, falling back to 👀 "your turn" when no token is present. Banner subtitle: Task complete / Task failed / Good news / Bad news / Turn complete. NOTE: depends on an out-of-repo instruction in the user's global CLAUDE.md.
- **Terminal-tab rename = FILE-WATCH, not `open`** (v1.7.0): hooks write `/tmp/cc-notify/<sid>.tab` (`{pids,name}`) via `cc_write_tab`; the extension `fs.watch`es the dir and renames via `renameWithArg`. **Why not `open`:** `open -g "<scheme>://…"` STILL activates the editor and yanks Aerospace focus across workspaces (a URL scheme forces app activation even with `-g`) — that was the "window randomly grabs focus" bug. `renameWithArg` only acts on the active terminal and does NOT raise the window, so the extension renames when the target is active, else defers to `onDidChangeActiveTerminal` — **never `show()`** (show raises the window). Native tab *color* is unsettable via API → emoji-only. See @LESSONS.md #16/#17.
- **Window capture is whitelisted** (v1.7.0): `cc-capture-window.sh` only saves the focused window as the jump-back target if its app is a terminal/editor (Cursor/Code/Terminal/iTerm2/Ghostty/…). Sessions driving Chrome (browser automation) were capturing Chrome → click focused Chrome. See @LESSONS.md #16.
- **Banner icon/color** (v1.7.0): orange Claude mark always shown as `--content-image` (right thumbnail). The left icon can only be changed via `--sender` impersonation (Big Sur+ ignores `--app-icon`), BUT **macOS silently drops notifications from a bundle id lacking notification permission**, and `com.anthropic.claudefordesktop` is unauthorized on this machine (confirmed absent from `~/Library/Preferences/com.apple.ncprefs.plist`). Impersonating it killed all banners and left only Claude Code's `terminal_bell` — the v1.7.0 "no notifications" regression. So `--sender` is now **opt-in** via `~/.claude/notify.claude_icon` (default off → default authorized sender → reliable banners). See @LESSONS.md #14. Diagnose with `bin/cc-notify-doctor`.
- **Stop gating** (v1.7.0: fires immediately by default): Stop = "fully done" (subagents already returned, bg shells/watchers don't hold the turn), so we fire on it instead of relying on CC's ~60s idle Notification. Two opt-outs, both OFF by default: `notify.disable_stop` (kill-switch) and `notify.suppress_when_focused` (only then run the frontmost check → suppress if the captured target window is Aerospace-focused; app-level frontmost is the fallback). Frontmost suppression was inverted to opt-in because its detection is unreliable in VS Code/Cursor and ate legitimate pings.
- **PPID walk first, then tty walks**: handles both non-tmux (`$TMUX` not set or stripped) and tmux-attached (PPID hits launchd-parented server). See @LESSONS.md gotcha #11 about multi-client tmux iteration.
- **Hotkey fires on key release**: prevents key-repeat from cycling through all live banners. Plus 800ms script-level debounce.
- **alerter timeout = 120s**: gives time to react to the hotkey.

## Known issues to pick up

1. **tmux-watch monitor clients** — see @TODO.md. Sessions with only monitor clients (no real Terminal attached) can't be focused. Options: (a) skip monitor-client ttys when iterating, (b) spawn new Terminal as fallback, (c) refactor tmux-watch to mark its clients.
2. **VS Code / Cursor pane focus** — SOLVED in v1.7.0 via the `editor-extension/` (`terminal.show()` matched by shell pid). Requires the extension installed + window reloaded. Without it, falls back to window-level focus only (the URI is suppressed when the extension dir is absent).
3. **Two Cursor windows with the same workspace basename** — workspace-folder-basename matching picks the first one. Rare. Could disambiguate via `cursor --status` window list if it becomes an issue.

## Karabiner config

Rule at `~/.config/karabiner/karabiner.json` profile[0].complex_modifications.rules — search for "cc-notify". The old stable `marketplaces/…/plugins/cc-notify/bin/…` path is DEAD (marketplace no longer bundles cc-notify — see v1.7.11). Now resolves dynamically (dev repo first, else newest cache) so it survives version bumps:
```
bash -lc 'for p in "$HOME"/Projects/cc-notify/bin/cc-banner-click $(ls -dt "$HOME"/.claude/plugins/cache/farishijazi-plugins/cc-notify/*/bin/cc-banner-click 2>/dev/null); do [ -x "$p" ] && exec "$p"; done'
```
(README ships the cache-only variant for users without a dev checkout.)

## Debug tips when resuming

To re-instrument the diagnostic logging that was here during v1.4-v1.5 debugging:
- Stop gating decisions → add `printf '[%s] DEBUG_GATE event=%s gui_pid=%s claude_wid=%s focused_wid=%s\n' ...` near top of Stop block in cc-notify.sh
- tmux_jump steps → log return codes of switch-client / select-window / select-pane to `/tmp/cc-notify.tmux.log`
- Hotkey path → cc-banner-click already logs picked session_id + live alerters to `/tmp/cc-notify.gate.log`

Useful one-liner to see live state:
```bash
ls -t /tmp/cc-notify/*.route | head -3 | xargs -I{} sh -c 'echo "--- {} ---"; cat {}'
pgrep -fl 'alerter.*cc-'
tmux list-clients -F '#{client_tty} #{session_name} #{?client_focused,focused,unfocused}'
aerospace list-windows --focused --format '%{window-id}|%{app-name}|%{window-title}'
```

## What to verify on resume

1. **Cursor click-back lands on the right workspace window** (v1.6.0 workspace-folder match). Test by clicking from a Claude session whose cwd is a subdir of an open Cursor workspace — should focus that workspace's window, not re-open the subdir.
2. **Terminal-pane focus (v1.7.0)** — install the extension (`bin/cc-install-editor-extension`), reload the editor window, then fire `open "cursor://farishijazi.cc-notify-focus/focus?pids=<shell_pids from route>"` and check `$TMPDIR/cc-notify-focus.last` says `matched pid=…`. Confirm the *exact* pane (with multiple terminals open + tmux inside it) gets focus, not just the panel.
3. **Stop suppression works correctly in side-by-side layouts** (already verified for Terminal.app, untested for Cursor).
4. **autoUpdate pulls latest cache** — currently the cache dirs are 1.0.0–1.4.0; newer versions arrive on next session start with autoUpdate. All existing caches have been hand-patched.
